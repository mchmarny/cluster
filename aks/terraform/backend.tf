# Terraform state backend configuration
# Uncomment and configure after running ./tools/setup
# or initialize with: terraform init -backend-config=backend.tfvars

# terraform {
#   backend "azurerm" {
#     # Configuration provided via backend-config file or command-line flags:
#     # resource_group_name  = "rg-terraform-state"
#     # storage_account_name = "statfstatexxx"
#     # container_name       = "tfstate"
#     # key                  = "aks-<cluster-name>.tfstate"
#   }
# }
