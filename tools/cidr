#!/bin/bash
set -euo pipefail

# Print current egress IP as a CIDR
# Usage: tools/cidr

IP=$(curl -s https://checkip.amazonaws.com | tr -d '[:space:]')
[[ -z "$IP" ]] && echo "error: failed to detect egress IP" >&2 && exit 1
echo "${IP}/32"
